---
id: "OPS-009"
title: "Harden OpenClaw service ownership and runtime verification"
status: "done"
priority: "high"
owner_agent: "ops"
agent_type: "ops"
created_at: "2026-03-10T19:38:33Z"
updated_at: "2026-03-10T19:38:33Z"
source: "codex-session"
depends_on:
- "QUE-001"
- "QUE-002"
blocked_by:
tags:
- "runtime"
- "launchd"
- "hardening"
- "verification"
artifacts:
- "/Volumes/Storage/OpenClaw/scripts/verify-openclaw-runtime.sh"
- "/Users/cderamos/.openclaw/disabled/launchagents-20260310"
- "/Users/cderamos/Library/LaunchAgents/com.visualgraphx.openclaw.device-autoapprove.plist"
- "/Users/cderamos/.openclaw/bin/openclaw-device-autoapprove.py"
- "/Users/cderamos/.openclaw/openclaw-device-autoapprove-policy.json"
---

## Summary

Removed stale launchd owners from older antigravity paths, kept only the intended gateway/dashboard/Cloudflare tunnel services, and added a one-command runtime verification script so recovery is based on checks rather than restart optimism.

## Current State

- Active launchd services are now `ai.openclaw.gateway`, `ai.openclaw.dashboard`, and `com.visualgraphx.cloudflared.cd`.
- Stale launch agents were moved into `/Users/cderamos/.openclaw/disabled/launchagents-20260310`.
- `/Volumes/Storage/OpenClaw/scripts/verify-openclaw-runtime.sh` verifies launchd state, listening ports, security audit status, and the absence of stale launch-agent files.
- Runtime directive subagent/team launches now use the local `openclaw agent` CLI path instead of the Gateway HTTP `/tools/invoke` exception path.
- A dedicated launchd watcher now auto-approves matching iPhone Control UI pairing requests so remote PWA access no longer depends on a manual Mac-side prompt.

## Acceptance

- [x] Stale antigravity launch agents are removed from the active LaunchAgents directory
- [x] Only the intended gateway, dashboard, and Cloudflare tunnel remain loaded
- [x] A repeatable runtime verification script exists and passes
- [x] Gateway status and security audit are rechecked after the changes

## Activity Log

- 2026-03-10T19:38:33Z cd: Archived stale antigravity/cloudflared launch agents, hardened the gateway config, approved the pending remote dashboard device, and verified the resulting runtime stack with `/Volumes/Storage/OpenClaw/scripts/verify-openclaw-runtime.sh`.
- 2026-03-10T19:55:00Z cd: Replaced runtime directive HTTP `sessions_spawn` usage with local `openclaw agent` launches, removed `gateway.tools.allow=["sessions_spawn"]`, restarted the gateway, and re-verified that `openclaw security audit --deep` now returns `0 critical · 0 warn`.
- 2026-03-10T21:16:00Z cd: Added `com.visualgraphx.openclaw.device-autoapprove` launchd service plus a narrow iPhone/webchat/operator policy, then revoked and observed automatic token rotation for the iPhone PWA device to prove the reconnect no longer depends on manual approval at the Mac.

## Notes

The remaining audit output is informational only (`browser control: enabled`, personal-operator trust model summary).
