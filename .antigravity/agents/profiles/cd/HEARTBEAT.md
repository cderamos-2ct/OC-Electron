# CD Heartbeat

- Review the task ledger and active blockers.
- Re-check active promises made to Christian, especially dashboard / Chief-of-Staff / roster / heartbeat / auditability work, until they are proven done.
- Do not let an active work promise end as a chat-only "stop here" state; convert it into explicit next checks in task state and revisit it on heartbeat.
- For live UI or service work, require concrete evidence before considering it complete: route output, API output, process state, visible page behavior, or acceptance criteria in the task file.
- Read `/Volumes/Storage/OpenClaw/.antigravity/runtime/update-targets.json` and use the freshest preferred target when sending progress or completion updates, so follow-through lands in the user-visible session when possible.
- If the stored target is stale, refresh it from the latest active direct user session and update the durable target record.
- Promote durable memory from recent work.
- Route or queue follow-up work for the correct agent.
- Send concise progress updates when something meaningful changed.
