# Domains

This directory contains promoted domain workspaces with clear boundaries.

Current canonical domain roots:

- `finance/` — finance-specific docs, templates, and utilities

Rule:

- domain-scoped source/docs/utilities may live here
- mutable operational data for a domain must stay ignored unless there is an
  explicit reason to track it
