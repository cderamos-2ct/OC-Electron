{
  "name": "@openclaw/gateway-client",
  "version": "0.1.0",
  "description": "OpenClaw Gateway WebSocket client — connection management, reconnection, and auth flow",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@openclaw/core": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "typescript": "^5.7.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
