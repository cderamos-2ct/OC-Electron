import React from 'react';

export function AgentStatusBar() {
  // Disabled — shows stale placeholder data without a live gateway.
  // Re-enable once real agent state flows through the gateway connection.
  return null;
}
